class Solution:
    def sortColors(self, nums: List[int]) -> None:
        n0=nums.count(0)
        n1=nums.count(1)
        n2=nums.count(2)
        for i in range(n0)
            nums[i]=
        for i in range(n0,n1+n0)
            nums[i]=
        for i in range(n1+n0,n2+n0+n1):
            nums[i]=2

