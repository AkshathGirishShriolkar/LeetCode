class Solution:
    def canJump(self, nums: List[int]) -> bool:
        index_of_last_true=len(nums)-1
        for i in range(len(nums)-2,-1,-1):    
            if i+nums[i]>=index_of_last_true:
                index_of_last_true=i
        return True if index_of_last_true==0 else False
